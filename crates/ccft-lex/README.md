# ccft-lex

Pure text fingerprinting: lexical stats (TTR / function-word / bigram
entropy), novelty, system-block stripping.

Part of the [`ccft`](https://github.com/adiled/ccft) workspace. Used to tell
driver content apart from bot/loop machinery on the wire.

MIT license.
