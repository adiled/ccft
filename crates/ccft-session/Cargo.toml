[package]
name = "ccft-session"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/adiled/ccft"
readme = "README.md"
keywords = ["ccft", "agentic", "proxy", "ledger", "flytrap"]
categories = ["development-tools::debugging", "network-programming"]
description = "Extract agent session ids from hyper headers + JSON metadata bodies (Claude Code, OpenAI, SDKs)"

[dependencies]
hyper = "1"
serde_json = "1"
sha1 = "0.10"
hex = "0.4"
